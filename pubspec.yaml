name: gg_dir_state
description: This package allows to track changes and state of git directories
version: 0.1.0
repository: https://github.com/ggsuite/gg_dir_state.git

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  path: ^1.9.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.2
